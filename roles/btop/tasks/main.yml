---
- name: load distribution specific vars (if any)
  ansible.builtin.include_vars: "{{ item }}"
  when_first_found:
    - files: ["{{ ansible_os_family | lower }}.yml"]
      paths: ["vars"]
      skip: true

- name: dispach operation
  ansible.builtin.include_tasks: "{{ op | default('install')}}.yml"
